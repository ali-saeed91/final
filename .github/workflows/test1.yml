on:
  pull_request:
    types:
      - closed

jobs:
  state_event_type:
    runs-on: ubuntu-latest
    steps:
    - name: if_pr
      if: github.event.pull_request
      run: |
        echo An issue was closed
        echo ${{github.event.issue.number}}
    - uses: actions/checkout@v3
    - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          #ISSUE_URL: ${{ github.event.issue.html_url }}
          -name: Upon Merge Approval Reasign to QA with Label
   
      run: |
          gh issue reopen "${{ github.event.issue.number }}"
          gh issue edit "${{ github.event.issue.number }}" --remove-assignee "@me"
          gh issue edit "${{ github.event.issue.number }}" --remove-label "bug"
          gh issue edit "${{ github.event.issue.number }}" --add-label "documentation"
          gh issue edit "${{ github.event.issue.number }}" --add-assignee ali-saeed91
